<div class="table-responsive col-md-6 col-md-offset-3">
    <table class="table table-striped">
        <thead>
            <tr>
                <th class="col-md-6">Name</th>
                <th class="col-md-3">Links</th>
            </tr>
        </thead>
        <tbody>
          <% @top_users.each do |top_user| %>
            <tr>
                <td><%=top_user.name%></td>
                <td>
                    <span class="label label-as-badge label-primary"><%=top_user.links.count%> links</span>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
</div>
